/**
 * @file src/renderer/hooks/index.ts
 * @description This folder contains custom React hooks.
 * Hooks encapsulate reusable logic that can be shared across components.
 * Example: useFetch, useWindowSize, useAuth.
 * 
 * Export your hooks here:
 * export { default as useMyHook } from './useMyHook';
 */

export { useRecipes } from './useRecipes';
