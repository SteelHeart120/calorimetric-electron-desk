/**
 * @file src/shared/index.ts
 * @description This folder contains code shared between the Main and Renderer processes.
 * This includes TypeScript types, constants, and utility functions that need to be
 * accessible from both sides of the Electron IPC boundary.
 */

export * from './types';
export * from './constants';
