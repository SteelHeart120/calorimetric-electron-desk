// Shared TypeScript types and interfaces used across main and renderer processes

export interface CalorimetricData {
  // Add your data types here
}

export type MessageType = 'info' | 'warning' | 'error' | 'success';
